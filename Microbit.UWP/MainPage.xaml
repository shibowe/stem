<Page
    x:Class="Microbit.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microbit.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="tblURITitle" Margin="24,12" FontSize="16.666"  Text="本地图片存放路径:"/>
        <StackPanel Grid.Row="1" Margin="24,0,0,0" Orientation="Horizontal">
            <TextBox x:Name="tbURI" Width="340" 
                     BorderBrush="Beige"
                     BorderThickness="1.5"
                     Text="" />
            <Button x:Name="btnInvokeAPI" 
                    Margin="6,0" 
                    Click="BtnInvokeAPI_Click"
                    Content="选择图片并识别" />
        </StackPanel>
        <TextBlock x:Name="tblResultsTitle" Grid.Row="2" Margin="24,12,0,6" FontSize="16.666" Text="Azure情感服务识别结果:"/>
        <TextBlock x:Name="tblResults" Grid.Row="3" TextWrapping="Wrap" Margin="24,0" IsColorFontEnabled="True" Text="" />
    </Grid>
</Page>
