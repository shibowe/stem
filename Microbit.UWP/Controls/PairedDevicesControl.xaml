<UserControl
    x:Class="Microbit.UWP.Controls.PairedDevicesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microbit.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="#F2F2F2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="tblTitle" Text="我的设备:" Style="{StaticResource PairedPageTitleStyle}" />
        <Button x:Name="btnConnect" Grid.Row="1" 
                Style="{StaticResource PairedPageDeviceNameStyle}" 
                Click="btnConnect_Click"
                Content="{Binding DeviceName}"/>
    </Grid>
</UserControl>
